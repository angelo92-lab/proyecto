import './bootstrap';

  
  console.log('Archivo app.js cargado.');
  
  document.addEventListener('DOMContentLoaded', () => {
    // Puedes agregar aquí código JS que manipule el DOM o tu frontend dinámico
});